{
  "name": "uzumaki",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "pnpm build:core && pnpm --filter playground start",
    "build:core": "cargo build --release -p uzumaki && bun scripts/copy-binary.ts --release",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
